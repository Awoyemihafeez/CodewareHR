@model IEnumerable<TalentAcquisition.Core.Domain.Industry>

@{
    ViewBag.Title = "Manage Industry Records";
    Layout = "~/Views/Shared/_NormalLayout.cshtml";
}

<ol class="breadcrumb">
    <li><a href="@Url.Action("Dashboard","Admin")">Home</a></li>
    <li class="active">Industries</li>
    <li class="active">All</li>
</ol>
<section class="container-fluid">
                <div class="container-fluid" style="margin-left:3px">
                    <p>
                        <a href="/Admin/manage_industries/Create" style="padding:0.6em; border-radius:10px" class="btn btn-default btn-xs"><img src="~/Assets/Icons/File-Plus.png" width="15" /> Create New Industry</a>
                    </p>
                </div>
                    <div class="container-fluid">
                        <div class="panel panel-primary">
                            <div class="panel-heading">
                                <p class="panel-title">
                                    <a data-toggle="collapse" href="#main" style="text-decoration:none">All Industries</a>
                                </p>
                            </div>
                            <div id="main" class="panel-collapse collapse in">
                                <div class="panel-body" style="overflow-y:scroll;height:450px">
                                    <table class="table" style="color:black">
                                        <tr>
                                            <th>
                                                @Html.DisplayNameFor(model => model.Name)
                                            </th>
                                            <th> @Html.DisplayName("Modifications")</th>
                                        </tr>

                                        @foreach (var item in Model)
                                        {
                                            <tr>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.Name)
                                                </td>
                                                <td>
                                                    @Html.ActionLink("Edit", "Edit", new { id = item.IndustryId }) |
                                                    @Html.ActionLink("Details", "Details", new { id = item.IndustryId }) |
                                                    @Html.ActionLink("Delete", "Delete", new { id = item.IndustryId })
                                                </td>
                                            </tr>
                                        }

                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                    
            </section>

