@model IEnumerable<TalentAcquisition.Core.Domain.JobRequisition>

<table class="table table-striped" id="RequisitionTable" style="font-size:90%">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.JobTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Location)
        </th>
        <th>
            Dated Published
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StartDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ClosingDate)
        </th>
        <th>Modification</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.JobTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @if ((int)item.Status == 2)
                {
                    @item.PublishedDate.ToShortDateString()
                }
                else
                {
                    <p> Unpublished </p>
                }
            </td>

            <td>
                @item.StartDate.ToShortDateString()
            </td>
            <td>
                @item.ClosingDate.ToShortDateString()
            </td>
            <td>
                @if ((int)item.Status == 2)
                {
                    <a href="@Url.RouteUrl("MatchSkill",new { id = item.JobRequisitionID, details = item.JobTitle })" class="">
                        Skill Matching |
                    </a>
                    
                    <a href="@Url.RouteUrl("RequisitionLink",new { id = item.JobRequisitionID, details = item.JobTitle })" class="">Details </a>
                    <a href="@Url.RouteUrl("Close",new { id = item.JobRequisitionID, details = item.JobTitle })" class="">
                        | Close
                    </a>
                }
                else
                {
                    <a href="@Url.RouteUrl("MatchSkill",new { id = item.JobRequisitionID, details = item.JobTitle })" class="">
                        Skill Matching |
                    </a>
                    <a href="@Url.RouteUrl("PublishLink",new { id = item.JobRequisitionID, details = item.JobTitle })" class="">
                        Publish |
                    </a>
                    <a href="@Url.RouteUrl("RequisitionLink",new { id = item.JobRequisitionID, details = item.JobTitle })" class="">Details </a>
                    <a href="@Url.RouteUrl("Close",new { id = item.JobRequisitionID, details = item.JobTitle })" class="">
                       | Close
                    </a>
                }

            </td>
        </tr>
    }

</table>

